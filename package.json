{
    "name": "my-app",
    "version": "1.0.0",
    "scripts": {
       "build": "npm --prefix frontend install --f && npm --prefix frontend run build"
    },
    "dependencies": {
      "concurrently": "^6.0.0"
    }
  }
